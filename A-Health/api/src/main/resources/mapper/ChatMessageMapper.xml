<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kmbeast.mapper.ChatMessageMapper">

    <resultMap id="BaseResultMap" type="com.kmbeast.pojo.entity.ChatMessage">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 获取指定会话的所有消息（按时间正序） -->
    <select id="getBySessionId" resultMap="BaseResultMap">
        SELECT id, session_id, role, content, create_time
        FROM chat_message
        WHERE session_id = #{sessionId}
        ORDER BY create_time ASC
    </select>

    <!-- 获取指定会话最新的N条消息（按时间正序） -->
    <select id="getRecentBySessionId" resultMap="BaseResultMap">
        SELECT id, session_id, role, content, create_time
        FROM (
            SELECT id, session_id, role, content, create_time
            FROM chat_message
            WHERE session_id = #{sessionId}
            ORDER BY create_time DESC
            LIMIT #{limit}
        ) sub
        ORDER BY create_time ASC
    </select>

    <!-- 插入消息 -->
    <insert id="insertMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (session_id, role, content, create_time)
        VALUES (#{sessionId}, #{role}, #{content}, #{createTime})
    </insert>

</mapper>
